# 메뉴 번역 SQL 실행 가이드

## 📋 실행 순서

### 1. Supabase Dashboard 접속
1. https://supabase.com 로그인
2. 프로젝트 선택
3. 좌측 메뉴에서 **SQL Editor** 클릭

### 2. SQL 실행
1. **New query** 버튼 클릭
2. `/database/1001_complete_menu_translations.sql` 파일 내용 전체 복사
3. 붙여넣기
4. **Run** 버튼 클릭 (또는 Ctrl+Enter)

### 3. 결과 확인
실행이 완료되면 다음과 같은 메시지가 표시됩니다:
```
========================================
✅ Menu translation completed successfully
========================================
Total menus: XX
Translated: XX
Remaining NULL: 0
========================================
```

### 4. 검증
다음 쿼리로 번역이 잘 되었는지 확인:
```sql
SELECT 
  menu_name,
  menu_name_en,
  parent_menu,
  parent_menu_en,
  description_en
FROM menu_permissions
ORDER BY display_order;
```

**예상 결과:**
- 모든 `menu_name_en` 값이 채워져 있어야 함
- NULL 값이 없어야 함
- 한글 메뉴명에 대응하는 영문 메뉴명 확인

## 🔍 SQL 파일 설명

### 수행 작업
1. **컬럼 추가**: `menu_name_en`, `parent_menu_en`, `description_en`
2. **메뉴 번역**: 모든 메뉴 항목을 한글→영문으로 번역
3. **NULL 처리**: 남은 NULL 값을 자동으로 채움
4. **인덱스 생성**: 성능 최적화를 위한 인덱스 추가
5. **검증**: 번역 완료 통계 출력

### 주요 메뉴 번역 예시

| 한글 | 영문 |
|------|------|
| 대시보드 | Dashboard |
| 회원 관리 | User Management |
| 파트너 관리 | Partner Management |
| 정산 관리 | Settlement Management |
| 입출금 관리 | Transaction Management |
| 게임 관리 | Game Management |
| 베팅 관리 | Betting Management |
| 고객지원 | Customer Support |
| 시스템 설정 | System Settings |

### 하위 메뉴 예시

| 부모 메뉴 (한글) | 하위 메뉴 (한글) | 부모 메뉴 (영문) | 하위 메뉴 (영문) |
|-----------------|----------------|-----------------|----------------|
| 회원 관리 | 회원 목록 | User Management | User List |
| 회원 관리 | 블랙리스트 | User Management | Blacklist |
| 파트너 관리 | 파트너 계층 | Partner Management | Partner Hierarchy |
| 정산 관리 | 수수료 정산 | Settlement Management | Commission Settlement |

## ⚠️ 주의사항

1. **백업 권장**: SQL 실행 전 데이터 백업 권장
2. **한 번만 실행**: 이 SQL은 한 번만 실행하면 됨
3. **중복 실행 안전**: 여러 번 실행해도 안전하게 설계됨 (UPDATE 문 사용)

## 🐛 문제 해결

### 문제: "column already exists" 에러
**해결:** 이미 컬럼이 존재한다는 뜻. 무시하고 계속 진행

### 문제: 일부 메뉴가 여전히 NULL
**해결:** 다음 쿼리로 수동 업데이트
```sql
UPDATE menu_permissions 
SET menu_name_en = menu_name
WHERE menu_name_en IS NULL;
```

### 문제: parent_menu_en이 NULL
**해결:** 부모 메뉴가 없는 최상위 메뉴는 정상. 하위 메뉴만 parent_menu_en 필요

## 📞 지원

번역 추가/수정이 필요한 경우:
1. `/database/1001_complete_menu_translations.sql` 파일 수정
2. 해당 메뉴의 UPDATE 문 찾기
3. `menu_name_en`과 `description_en` 값 수정
4. SQL 재실행
